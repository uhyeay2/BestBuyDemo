@model InsertProductViewModel

<h2>Create a New Product</h2>

@using (Html.BeginForm("InsertProduct", "Product", "Post"))
{
    <!-- Hide the ProductID from Form since the Database will assign that as needed. -->
    <input asp-for="Product.Guid" type="hidden" value="" />

    <!-- Get new product name -->
    <label asp-for="Product.Name" class="control-label">Name</label>
    <input type="text" name="Name" class="form-control" value="" />

    <!-- Get new produce price -->
    <label asp-for="Product.Price" class="control-label">Price</label>
    <input type="number" step="0.01" name="Price" class="form-control" />

    <!-- Get new product category from list of all categories in the database.-->
    <div class="form-group">
        <label for="">Select a Category</label>
        <select class="form-control" name="Category" value="">
            @foreach (var category in Model.CategoryNames)
            {
                <option value="@category">@category</option>
            }
        </select>
    </div>

    <!-- Submit Button -->
    <input type="submit" value="Create Product" class="btn btn-primary" />

}